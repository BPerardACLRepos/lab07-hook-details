import React from 'react';
import { Link } from 'react-router-dom';

const Header = () => (
    <nav>
        <Link to="/">
            CHARACTER LIST
        </Link>
    </nav>
);

export default Header;

